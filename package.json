{
  "name": "ai-finance-landing",
  "version": "0.1.0",
  "private": true,
  "description": "AI & Finance landing page - static HTML, deployed to Vercel",
  "scripts": {
    "dev": "npx serve . -l 3000",
    "deploy": "vercel --prod",
    "deploy:preview": "vercel"
  }
}
